# Allow native build scripts that the app actually relies on (esbuild, tailwind oxide)
# without prompting `pnpm install` for approval. Required for non-interactive CI / PaaS deploys.
auto-install-peers=true
strict-peer-dependencies=false
onlyBuiltDependencies[]=esbuild
onlyBuiltDependencies[]=@tailwindcss/oxide
